private var homePos: Vector3 = Vector3(0, 3, 0);
private var shaking: boolean = false;
private var noKeyPressedYet: boolean = true;

function Start () {

}

function Update () {
    /*
    if (shaking) {
        transform.localPosition = homePos;
        shaking = false;
    }

    if (Input.GetKey("a") && noKeyPressedYet) {
        transform.position.x -= 1f;
        shaking = true;
        noKeyPressedYet = false;
    }

    if (Input.GetKey("d") && noKeyPressedYet) {
        transform.position.x += 1f;
        shaking = true;
        noKeyPressedYet = false;
    }

                
    if (!Input.GetKey("a") && !Input.GetKey("d")) {
        noKeyPressedYet = true;
    }
    */
}